# v17.3 Juo source-backed production value promotion completion

## Status

Completion checkpoint for the Juo source-backed production value promotion metadata phase.

Current baseline:

- #199 v17.0 Juo source-backed production value promotion entry merged.
- #200 v17.1 Juo source-backed production value promotion fixture merged.
- #201 v17.2 Juo source-backed production value promotion guard hardening merged.
- Latest merge commit: `a73dbd0e7c31c29a4fa5748e798580596ac09de5`.

## Completed scope

The v17.x promotion phase added a typed, non-executable metadata layer after source-backed production value draft rows.

The source-backed production value promotion layer now records probability and reward promotion rows linked to source-backed production value draft rows.

The completed promotion metadata covers:

- source-backed production value promotion id
- source-backed production value draft id
- preflight id
- readiness review id
- production value placeholder id
- draft metadata id
- promotion gate id
- extracted value metadata id
- unit metadata id
- citation id
- source id
- source type
- value domain
- placeholder status
- preflight status
- source-backed value readiness status
- citation consistency status
- unit consistency status
- normalization consistency status
- conflict review status
- promotion decision status
- source-backed value creation eligibility
- draft status
- source-backed production value promotion status
- execution eligibility

Reward promotion rows additionally cover:

- accounting consistency status

## Validation coverage

The v17.x test coverage now asserts:

- one probability promotion row per probability draft row
- one reward promotion row per reward draft row
- promotion rows stay linked to draft rows
- promotion ids stay unique and domain-separated
- promotion rows remain non-executable
- promotion rows remain blocked from source-backed production value materialization
- materialized source-backed production value fields are not present
- numeric value fields are not present
- production graph bindings are not present
- runtime target substitution fields are not present
- expected value assertions are not present
- expected reward assertions are not present
- promotion statuses remain bounded to blocked pre-production states

## Preserved boundary

The v17.x promotion phase did not add or change:

- real Beast King / Juo probability values
- real Beast King / Juo reward values
- source-backed production value rows
- production fixture values
- executable production graph
- runtime target substitution
- expected value assertions
- report identity
- report adapters
- formatters
- contribution output
- UI
- TeX
- CSS
- Android native code
- Sugoroku expected reward baseline

## Current guarantee

A Juo source candidate can now pass through the following metadata layers without becoming executable:

1. source policy
2. candidate inventory
3. review gate
4. citation metadata
5. unit metadata
6. extracted value metadata
7. production value promotion gate
8. production value draft metadata
9. production value placeholder
10. production value readiness review
11. source-backed production value preflight
12. source-backed production value draft
13. source-backed production value promotion

The promotion layer can carry the metadata shape needed for later source-backed production value materialization.

At this checkpoint, source-backed value creation eligibility remains `no`, draft status remains `draft_blocked`, and promotion status remains `promotion_blocked`.

The layer still cannot provide source-backed numeric values or execute a production graph.

## Next step

The next phase should begin the source-backed production value materialization boundary.

That phase should still avoid inserting real Beast King / Juo probability values, real reward values, executable production graph bindings, runtime target substitution, and expected value assertions until the materialization metadata and guard coverage are complete.
