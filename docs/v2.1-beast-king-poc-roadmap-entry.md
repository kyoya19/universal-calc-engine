# v2.1 Beast King PoC roadmap entry

## Status

Roadmap entry recorded after the v2.0 reporting regression fixture stabilization checkpoint.

Current main baseline:

- #112 Document v2.0 reporting regression fixture stabilization checkpoint
  - merge commit: `d1765d52316585ec4de700ce8a9f472e21c7274b`
  - PR head CI run #226: success

## Lightweight schedule toward Beast King PoC

This roadmap treats Beast King PoC as a later machine-specific PoC entry, not the immediate next implementation step.

1. Reporting stabilization closeout
   - Keep public reporting surface and fixture regression behavior stable.
   - Limit changes to documentation-only or test-only stabilization.
   - Do not change solver, contribution, formatter, TeX, UI, CSS, or Android native behavior.

2. Generic PoC boundary refresh
   - Reconfirm the existing Sugoroku PoC as the generic state-transition reference.
   - Reconfirm expected reward baseline and target semantics.
   - Identify which parts are reusable for machine-specific PoC seeds.

3. Beast King PoC seed preparation
   - Add a documentation-only or fixture-only seed for Beast King PoC inputs.
   - Separate confirmed inputs, assumptions, and unknowns.
   - Keep machine-specific rules outside production runtime until fixture expectations are explicit.

4. Beast King PoC minimum implementation entry
   - Add the smallest test-only machine-specific fixture or adapter boundary.
   - Avoid broad runtime generalization until the seed behavior is locked by regression tests.

## Recommended PR sequence

- v2.1: roadmap entry only.
- v2.2: generic PoC boundary refresh documentation.
- v2.3: Beast King PoC seed documentation or fixture stub.
- v2.4: Beast King PoC minimum test-only fixture.
- v2.5: implementation only if the fixture contract is stable.

## Preserved semantics

- `solver target = transition.to`
- `contribution target = transition.to`
- `generatedTo` remains validation, diagnostics, summary, and comparison report data.
- `generatedTo` is not wired into solver target or contribution target semantics.
- Runtime target substitution remains out of scope.
- `generated_candidate_as_solver_target` remains a rejected runtime policy.

## Preserved expected reward baseline

- start / position 0: `2.25`
- position 1: `1.5`
- position 2: `1`
- position 3: `0`

## Boundaries

- Documentation-only roadmap entry.
- No production source change.
- No public reporting type change.
- No adapter behavior change.
- No formatter behavior change.
- No row-level regression expectation change.
- No runtime target substitution.
- No generated target solver or contribution wiring.
- No UI framework, CSS, Android native, or TeX rendering change.
- No Beast King runtime implementation in this step.

## Next recommended entry

Proceed to a generic PoC boundary refresh before adding Beast King-specific fixtures. Keep the next step documentation-only or test-only, and continue separating confirmed inputs, assumptions, and unknowns before any machine-specific runtime implementation.
