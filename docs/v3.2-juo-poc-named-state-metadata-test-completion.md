# v3.2 Juo PoC named-state metadata test completion

## Status

Completion checkpoint recorded after the v3.2 Juo PoC named-state metadata alignment validation.

Current main baseline:

- #126 Add Juo named-state metadata alignment tests
  - merge commit: `6cbbb49bc3300652efb6278a6e0e2604c1441f14`
  - CI run #254: success

## Completed scope

- Extended `packages/core/test/juo_poc_fixture.test.ts`.
- Added test-only metadata alignment coverage for `juoPocNamedStateModel`.
- Confirmed each named state keeps its id and properties aligned with the declared placeholder stage:
  - `id = juoStateId(stage)`
  - `properties.machine = juo`
  - `properties.stage = stage`
- Confirmed each named-state transition effect updates `stage` to match the transition target.

## Boundary preserved

- `juoPocSeedModel` remains unchanged as the minimal placeholder fixture.
- `juoPocNamedStateModel` remains test-only.
- Named-state fixture values remain placeholders.
- Machine-specific probability values remain unresolved.
- Machine-specific reward values remain unresolved.
- Machine-specific expected values remain unresolved.
- Named stages remain placeholder labels, not production semantics.

## Generic semantics preserved

- `solver target = transition.to`
- `contribution target = transition.to`
- `generatedTo` remains validation, diagnostics, summary, and comparison report data.
- `generatedTo` is not wired into solver target or contribution target semantics.
- Runtime target substitution remains out of scope.

## Generic expected reward baseline preserved

- start / position 0: `2.25`
- position 1: `1.5`
- position 2: `1`
- position 3: `0`

## Out of scope

- Production source change.
- Beast King / Juo production expected value implementation.
- Machine-specific probability or reward insertion.
- Reporting adapter connection.
- Formatter change.
- Runtime target substitution.
- UI framework, CSS, Android native, or TeX rendering change.

## Next recommended entry

Proceed with a narrow test-only fixture transition-order or graph-shape checkpoint if additional hardening is useful, or stop before machine-specific production inputs until confirmed values are available.
