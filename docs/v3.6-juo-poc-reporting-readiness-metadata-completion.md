# v3.6 Juo PoC reporting-readiness metadata completion

## Status

Completion checkpoint recorded after the v3.6 Juo PoC reporting-readiness metadata checkpoint.

Current main baseline:

- #133 Add Juo reporting readiness metadata test
  - merge commit: `928f5b37a709e1ff8286a922966ee2bfaa447552`
  - CI run #269: success

## Completed scope

- Extended `packages/core/test/juo_poc_fixture.test.ts`.
- Added a test-only reporting-readiness metadata checkpoint for `juoPocNamedStateModel`.
- Confirmed unresolved reporting-related metadata remains explicit:
  - `Machine-specific validation rules`
  - `Machine-specific expected report rows`
- Confirmed `juoPocNamedStateModel` states still expose only placeholder structural properties:
  - `machine`
  - `stage`
- Confirmed `juoPocNamedStateModel` transitions still expose only generic transition fields:
  - `from`
  - `to`
  - `probability`
  - `reward`
  - `effects`

## Boundary preserved

- `juoPocSeedModel` remains unchanged as the minimal placeholder fixture.
- `juoPocNamedStateModel` remains placeholder-only.
- Reporting-readiness remains metadata-only.
- No report rows are introduced.
- No report adapter is connected.
- No formatter surface is expanded.
- Machine-specific validation rules remain unresolved.
- Machine-specific expected report rows remain unresolved.

## Generic semantics preserved

- `solver target = transition.to`
- `contribution target = transition.to`
- `generatedTo` remains validation, diagnostics, summary, and comparison report data.
- `generatedTo` is not wired into solver target or contribution target semantics.
- Runtime target substitution remains out of scope.

## Generic expected reward baseline preserved

- start / position 0: `2.25`
- position 1: `1.5`
- position 2: `1`
- position 3: `0`

## Out of scope

- Production source change.
- Beast King / Juo production expected value implementation.
- Machine-specific probability or reward insertion.
- Reporting adapter connection.
- Formatter change.
- Contribution output change.
- Runtime target substitution.
- UI framework, CSS, Android native, or TeX rendering change.

## Next recommended entry

Proceed with a narrow v3.7 checkpoint around named-state fixture diagnostics compatibility, or stop before any machine-specific production inputs until confirmed values are available.
