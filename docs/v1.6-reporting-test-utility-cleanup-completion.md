# v1.6 reporting test utility cleanup completion

## Status

Completed after v1.5 reporting diagnostic fixture cleanup.

Merged PR:

- #104 Deduplicate compact report row test helper
  - merge commit: `82a6174b3b8413bf8c3a9954cbadfe81d94b044e`
  - CI run #209: success

## Scope completed

- Added shared test-only `compactRows()` helper in `packages/core/test/fixtures/report_rows.ts`.
- Reused the shared helper from:
  - `packages/core/test/report_model_adapter_regression.test.ts`
  - `packages/core/test/report_model_mismatch_adapter_regression.test.ts`
- Removed duplicated local `compactRows()` definitions from reporting regression tests.
- Kept existing row-level regression expectations unchanged.

## Preserved boundaries

- Test utility cleanup only.
- No production source change.
- No public reporting type change.
- No formatter behavior change.
- No runtime target substitution.
- No UI, CSS, Android native, or TeX rendering change.
- Existing expected reward baseline remains unchanged.

## Next recommended entry

Proceed to v1.7 reporting regression fixture naming cleanup only if the next step remains test-only and preserves current regression expectations. Avoid runtime target substitution and production reporting changes.
