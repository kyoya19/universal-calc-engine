# v1.5 reporting diagnostic fixture cleanup completion

## Status

Completed after v1.4 reporting diagnostic fixture coverage.

Merged PR:

- #102 Deduplicate mismatch reporting fixture setup
  - merge commit: `d7fe8d0f8e77daee5722c8bf155a6f688216ace1`
  - CI run #205: success

## Scope completed

- Added `buildMismatchedGeneratedTargetSolverGateResultFixture()`.
- Reused the shared mismatch solver gate result fixture from:
  - `buildMismatchedGeneratedTargetComparisonReportModelFixture()`
  - `buildMismatchedGeneratedTargetSolverGateSummaryReportModelFixture()`
- Kept existing mismatch regression expectations unchanged.

## Preserved boundaries

- Test fixture cleanup only.
- No production source change.
- No public reporting type change.
- No formatter behavior change.
- No runtime target substitution.
- No UI, CSS, Android native, or TeX rendering change.
- Existing expected reward baseline remains unchanged.

## Next recommended entry

Proceed to v1.6 reporting test utility cleanup only if the next step is to reduce duplicated `compactRows()` helpers across reporting tests. Keep it test-only.
