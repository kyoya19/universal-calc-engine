# v6.0 Juo source candidate inventory entry

## Status

Entry checkpoint for the Juo source candidate inventory phase.

Current baseline:

- #154 v5.3 source policy validation completion merged.
- Merge commit: `6719aaee1eb032f69eade0ba2e47fbc4068ab96e`.

## Purpose

This phase inventories candidate sources for real Beast King / Juo production input without inserting real probability or reward values.

The goal is to classify possible sources under the v5.1 source policy before any value is promoted to executable production input.

## Inventory scope

The inventory may record candidate source categories, expected citation fields, unresolved source gaps, and review gates.

Allowed candidate categories:

- primary
- secondary
- observed
- inferred
- formula
- assumption
- unknown
- excluded

## Required inventory fields

Each candidate source entry should record:

- source id
- source type
- source title or label
- expected value category
- expected unit category
- citation availability
- retrieval or observation status
- transcription status
- confidence status
- conflict status
- execution eligibility

## Boundary

This entry is documentation-only.

No real Beast King / Juo probability values, reward values, production fixture values, expected value assertions, executable production graph, runtime target substitution, report adapter changes, formatter changes, contribution output changes, UI changes, TeX changes, CSS changes, Android native changes, or Sugoroku baseline changes are included.

## Next step

The next step should add a concrete candidate inventory fixture or document that lists source candidates as non-executable rows.