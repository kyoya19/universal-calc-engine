# v3.5 Juo PoC output-shape checkpoint completion

## Status

Completion checkpoint recorded after the v3.5 Juo PoC output-shape checkpoint.

Current main baseline:

- #132 Add Juo named-state output shape test
  - merge commit: `398c0cdc65e3988315d1926619f41cf117572f13`
  - CI run #266: success

## Completed scope

- Extended `packages/core/test/juo_poc_fixture.test.ts`.
- Added a test-only output-shape checkpoint for `juoPocNamedStateModel`.
- Confirmed the generic expected reward pipeline still produces an `OutputResult` for the named-state placeholder fixture.
- Fixed the named-state output keys to:
  - `startState`
  - `expectedReward`
  - `expectedRewardByState`
- Confirmed `output.startState` remains aligned with `juoPocNamedStateModel.startState`.
- Confirmed `output.expectedReward` remains `0`.
- Confirmed `output.expectedRewardByState` is closed over the declared named-state placeholder set.
- Confirmed every named-stage expected reward remains `0`.

## Boundary preserved

- `juoPocSeedModel` remains unchanged as the minimal placeholder fixture.
- `juoPocNamedStateModel` remains a placeholder-only named-state fixture.
- Named-stage labels remain structural placeholders, not production semantics.
- Placeholder probabilities remain `1`.
- Placeholder rewards remain `0`.
- Machine-specific probability values remain unresolved.
- Machine-specific reward values remain unresolved.
- Machine-specific expected values remain unresolved.

## Generic semantics preserved

- `solver target = transition.to`
- `contribution target = transition.to`
- `generatedTo` remains validation, diagnostics, summary, and comparison report data.
- `generatedTo` is not wired into solver target or contribution target semantics.
- Runtime target substitution remains out of scope.

## Generic expected reward baseline preserved

- start / position 0: `2.25`
- position 1: `1.5`
- position 2: `1`
- position 3: `0`

## Out of scope

- Production source change.
- Beast King / Juo production expected value implementation.
- Machine-specific probability or reward insertion.
- Reporting adapter connection.
- Formatter change.
- Contribution output change.
- Runtime target substitution.
- UI framework, CSS, Android native, or TeX rendering change.

## Next recommended entry

Proceed with a narrow v3.6 checkpoint around named-state reporting-readiness metadata or stop before machine-specific production inputs until confirmed values are available.
