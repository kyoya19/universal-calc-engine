# v4.8 Juo production input stub validation hardening completion

## Status

Completion checkpoint recorded after the v4.7 Juo production-input stub validation hardening entry.

Current main baseline:

- #147 Document v4.6 Juo production input stub validation completion
  - merge commit: `fa636c340166384565f90b49bcf54281ca252a92`
- #148 Harden Juo production input stub validation tests
  - merge commit: `a6474c0b16d09f3abf62601a37c83b57609d5821`
  - CI run #299: success

## Completed scope

The v4.7 checkpoint hardened the Juo production-input stub validation tests against accidental executable production-value insertion.

Completed scope:

- Added forbidden production-value key checks.
- Rejected executable probability-like fields on probability rows.
- Rejected executable reward-like fields on reward rows.
- Rejected executable probability / reward fields on transition rows.
- Locked the expected key set for probability rows.
- Locked the expected key set for reward rows.
- Locked the expected key set for transition rows.

## Boundary preserved

The v4.7 hardening tests did not introduce production behavior.

Preserved boundary:

- No production source was changed.
- No fixture production value was added.
- No real Beast King / Juo probability value was inserted.
- No real Beast King / Juo reward value was inserted.
- No Juo expected value assertion was added.
- No executable production state graph was introduced.
- No executable production transition graph was introduced.
- No report adapter was changed.
- No report kind was changed.
- No report title was changed.
- No formatter was changed.
- No contribution output was changed.
- No runtime target substitution was introduced.
- No TeX, CSS, Android native, or UI rendering behavior was changed.
- Existing Sugoroku expected reward baseline remains unchanged.

## Hardened validation semantics

The following executable production-value keys are forbidden in the fixture stub rows covered by v4.7:

- `value`
- `numerator`
- `denominator`
- `decimalValue`
- `decimal_value`
- `formula`
- `payout`
- `netPayout`
- `expectedCoins`
- `expectedValue`
- `probability`
- `reward`

Probability rows are limited to:

- `normalizationGroup`
- `probabilityId`
- `sourceNote`
- `valueStatus`
- `valueType`

Reward rows are limited to:

- `accountingExclusions`
- `rewardId`
- `rewardUnit`
- `sourceNote`
- `valueStatus`
- `valueType`

Transition rows are limited to:

- `displayLabel`
- `fromStateId`
- `generatedTo`
- `generatedToStatus`
- `probabilityRef`
- `rewardRef`
- `toStateId`
- `transitionId`

## Remaining unresolved items

The following remain unresolved after v4.8:

- Real machine state list.
- Real terminal-state definition.
- Real transition graph.
- Probability source and citation policy.
- Reward source and unit policy.
- Whether inferred values are allowed.
- Whether formula values are allowed.
- Real probability normalization enforcement.
- Real reward-unit arithmetic enforcement.
- Whether Juo needs machine-specific report rows.
- Whether Juo needs a dedicated report adapter later.

## Next recommended checkpoint

- `v4.9 Juo production input stub validation hardening closure`

The v4.9 checkpoint should close the current hardening sequence and define the entry criteria for any later real production-input phase.
