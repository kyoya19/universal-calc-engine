# v3.0 Juo PoC named-state fixture completion

## Status

Completion checkpoint recorded after the v3.0 Juo PoC named-state fixture entry.

Current main baseline:

- #122 Add Juo PoC named-state fixture
  - merge commit: `6c3a2d732fb92a4be1d38324c5214b492f087897`
  - CI run #246: success

## Completed scope

- Extended `packages/core/test/fixtures/juo.ts`.
- Added `juoPocNamedStages` as a test fixture stage list:
  - `start`
  - `normal`
  - `chance`
  - `bonus`
  - `terminal`
- Added `juoPocNamedStateModel` as a separate fixture from the existing minimal placeholder fixture.
- Extended `packages/core/test/juo_poc_fixture.test.ts`.
- Confirmed the named-state fixture remains placeholder-only:
  - probability: `1`
  - reward: `0`
- Confirmed the named-state fixture solves through the generic expected reward pipeline as zero placeholder reward.

## Boundary preserved

- `juoPocSeedModel` remains unchanged as the minimal placeholder fixture.
- The named-state fixture is test-only.
- Machine-specific probability values remain unresolved.
- Machine-specific reward values remain unresolved.
- Machine-specific expected values remain unresolved.
- The added state names are labels for PoC structure only, not confirmed production semantics.

## Generic semantics preserved

- `solver target = transition.to`
- `contribution target = transition.to`
- `generatedTo` remains validation, diagnostics, summary, and comparison report data.
- `generatedTo` is not wired into solver target or contribution target semantics.
- Runtime target substitution remains out of scope.

## Generic expected reward baseline preserved

- start / position 0: `2.25`
- position 1: `1.5`
- position 2: `1`
- position 3: `0`

## Out of scope

- Production source change.
- Beast King / Juo production expected value implementation.
- Machine-specific probability or reward insertion.
- Reporting adapter connection.
- Formatter change.
- Runtime target substitution.
- UI framework, CSS, Android native, or TeX rendering change.

## Next recommended entry

Proceed with a narrow test-only named-state boundary validation if more fixture shape hardening is useful, or stop before machine-specific production inputs until confirmed values are available.
