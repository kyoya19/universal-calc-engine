# v21.3 Juo materialization approval boundary completion

## Status

Completion checkpoint for the Juo materialization approval boundary metadata phase.

Current baseline:

- #215 v21.0 Juo materialization approval boundary entry merged.
- #216 v21.1 Juo materialization approval boundary fixture / validation test merged.
- #217 v21.2 Juo materialization approval boundary guard hardening merged.
- Latest merge commit: `3c29ffcecc7abca6ddb9b5e2948e26ae47a8eb3a`.

## Completed scope

The v21.x phase added a typed, non-executable metadata layer after guarded source-backed production value materialization candidate review boundary rows.

The approval boundary layer now records probability and reward approval boundary rows linked to guarded candidate review boundary rows.

The completed approval boundary metadata covers:

- materialization approval boundary id
- guarded candidate review boundary id
- carried source, citation, unit, extracted value, draft, promotion, readiness, preflight, materialization, candidate, and review metadata
- approval eligibility
- materialization approval boundary status
- execution eligibility

Reward approval boundary rows additionally carry accounting consistency status.

## Validation coverage

The v21.x tests assert:

- one approval boundary row per guarded candidate review boundary row
- approval boundary rows stay linked to guarded candidate review boundary rows
- approval boundary ids stay unique and domain-separated
- approval boundary rows remain non-executable
- approval eligibility remains `no`
- materialization approval boundary status remains `approval_blocked`
- execution eligibility remains `no`
- production value fields are not present
- numeric value fields are not present
- graph binding fields are not present
- runtime substitution fields are not present
- expected value assertion fields are not present
- expected reward assertion fields are not present
- approval boundary row key sets are closed

## Preserved boundary

The v21.x phase did not add or change:

- real Beast King / Juo probability values
- real Beast King / Juo reward values
- source-backed production value rows
- production fixture values
- executable production graph
- runtime target substitution
- expected value assertions
- report identity
- UI, TeX, CSS, or Android native code
- Sugoroku expected reward baseline

## Current guarantee

A Juo source candidate can now pass through the materialization approval boundary without becoming executable.

At this checkpoint, source-backed value creation eligibility remains `no`, materialization eligibility remains `no`, materialization candidate eligibility remains `no`, candidate review eligibility remains `no`, approval eligibility remains `no`, materialization approval boundary status remains `approval_blocked`, and execution eligibility remains `no`.

The layer still cannot provide source-backed numeric values or execute a production graph.

## Next step

The next phase should define a post-approval materialization decision boundary or equivalent final pre-materialization metadata layer.

That phase should still avoid inserting real Beast King / Juo probability values, real reward values, executable production graph bindings, runtime target substitution, and expected value assertions until decision metadata and guard coverage are complete.
