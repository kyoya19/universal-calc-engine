# v1.2 reporting fixture / adapter regression consolidation completion

## Status

Completed after the v1.1 reporting model shape hardening checkpoint.

Merged PR:

- #96 Consolidate reporting adapter fixture helpers
  - merge commit: `ae5545b65a3f0ed428d2ccf47ba21defb6ce12a0`
  - CI run #189: success

## Scope completed

- Added test-only reporting fixture helpers in `packages/core/test/fixtures/reporting.ts`.
- Consolidated generated-target comparison report model setup behind a shared fixture helper.
- Consolidated accepted solver gate summary report model setup behind a shared fixture helper.
- Consolidated rejected solver gate summary report model setup behind a shared fixture helper.
- Reused the helpers from `packages/core/test/report_model_shape.test.ts`.
- Kept the existing report model shape invariant expectations unchanged.
- Kept plain text fallback regression coverage unchanged.

## Public reporting surface preserved

This checkpoint does not change the public reporting surface:

- `ReportModel`
- `ReportSection`
- `ReportRow`
- `ReportRowStatus`
- `formatReportModelPlainText()`
- `generatedTargetComparisonReportToReportModel()`
- `generatedTargetSolverGateResultSummaryToReportModel()`

## Runtime and modeling boundaries preserved

This checkpoint does not introduce runtime target substitution.

The following boundaries remain fixed:

- `solver target = transition.to`
- `contribution target = transition.to`
- `generatedTo` remains limited to validation, diagnostics, summary, and comparison reporting.
- `generatedTo` is not connected to solver target or contribution target.
- `generated_candidate_as_solver_target` remains rejected runtime policy.
- Expected reward baseline remains:
  - start / position 0: `2.25`
  - position 1: `1.5`
  - position 2: `1`
  - position 3: `0`

## Non-goals preserved

This checkpoint intentionally does not change:

- UI framework code
- CSS guidance
- Android native rendering
- TeX rendering
- Runtime target substitution
- Contribution output semantics
- Existing formatter behavior

## Next recommended entry

Proceed to v1.3 reporting regression snapshot coverage only if the next step needs stronger adapter output lock-in.

Safe candidates:

1. Add focused snapshot-style expectations for generated-target comparison report model rows.
2. Add focused snapshot-style expectations for accepted/rejected gate summary report rows.
3. Keep all additions test-only.
4. Avoid runtime target substitution.
5. Avoid UI/CSS/Android native work.
6. Avoid TeX rendering changes.
