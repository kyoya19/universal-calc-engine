# v1.3 reporting regression snapshot coverage completion

## Status

Completed after v1.2 reporting fixture consolidation.

Merged PR:

- #98 Add report model adapter regression snapshots
  - merge commit: `c4c5034c0779cdf1666087c99cca9330299aed51`
  - CI run #195: success

## Scope completed

- Added `packages/core/test/report_model_adapter_regression.test.ts`.
- Added test-only regression coverage for generated-target comparison report model rows.
- Added test-only regression coverage for accepted solver gate summary report model rows.
- Added test-only regression coverage for rejected solver gate summary report model rows.
- Locked adapter row `id`, `label`, `plainText`, `status`, and `metadata` output.

## Validation note

The rejected fixture removes `effects[]` from the first transition. The graph still reaches position 2 through the other start transition, so the rejected summary uses:

- `edgeCount: 4`
- `generatedTargetReadyEdgeCount: 3`
- `rejectionCode: missing_generated_target`
- `rejectionType: missing_generated_target`

## Preserved boundaries

- No production source change.
- No public reporting type change.
- No formatter behavior change.
- No UI, CSS, Android native, or TeX rendering change.
- Existing expected reward baseline remains unchanged.

## Next recommended entry

Proceed to v1.4 diagnostic fixture coverage only if stronger mismatch/rejection reporting lock-in is needed.
