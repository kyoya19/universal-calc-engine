# v2.6 Juo PoC minimum test completion

## Status

Completion checkpoint recorded after the v2.5 Juo PoC minimum test entry.

Current main baseline:

- #117 Add Juo PoC fixture minimum solver test
  - merge commit: `fa56a7cacfd3d99822c2ff247c7418997fc61eb6`
  - CI run #236: success

## Completed scope

- Added `packages/core/test/juo_poc_fixture.test.ts`.
- Kept the change test-only.
- Ran `juoPocSeedModel` through:
  - `expandModel()`
  - `evaluateModel()`
  - `solveExpectedReward()`
  - `toOutputResult()`
- Confirmed the placeholder expected reward output remains `0` for:
  - `start`
  - `placeholder_terminal`
- Confirmed Juo-specific probability, reward, and expected values remain unresolved.

## Boundary preserved

- `juoPocSeedModel` remains a placeholder fixture.
- `probability: 1` remains a placeholder value.
- `reward: 0` remains a placeholder value.
- Juo-specific report rows are not fixed.
- Juo-specific production values are not claimed.

## Generic semantics preserved

- `solver target = transition.to`
- `contribution target = transition.to`
- `generatedTo` remains validation, diagnostics, summary, and comparison report data.
- `generatedTo` is not wired into solver target or contribution target semantics.
- Runtime target substitution remains out of scope.
- `generated_candidate_as_solver_target` remains a rejected runtime policy.

## Generic expected reward baseline preserved

The existing generic Sugoroku baseline remains unchanged:

- start / position 0: `2.25`
- position 1: `1.5`
- position 2: `1`
- position 3: `0`

## Out of scope

- Production source change.
- Public reporting type change.
- Adapter behavior change.
- Formatter behavior change.
- Runtime target substitution.
- Generated target solver or contribution wiring.
- UI framework, CSS, Android native, or TeX rendering change.

## Next recommended entry

Proceed with a narrow Juo PoC validation test only if it remains test-only and keeps machine-specific values unresolved.
