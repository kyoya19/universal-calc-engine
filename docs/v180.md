# v18.0 Juo source-backed production value materialization boundary entry

## Status

Entry checkpoint for the Juo source-backed production value materialization boundary phase.

Current baseline:

- #202 v17.3 Juo source-backed production value promotion completion merged.
- Merge commit: `8d21627acc804ab73993c2797cf1da80574a3750`.

## Purpose

This phase defines the boundary between source-backed production value promotion metadata and later source-backed production value materialization.

The phase is still documentation-only at entry.

It does not materialize real Beast King / Juo probability values or reward values.

## Materialization boundary scope

A later source-backed production value materialization layer may be allowed to describe the shape of materialization candidates after promotion, but this entry checkpoint does not create those rows yet.

The boundary is intended to separate:

- promoted source-backed production value metadata
- source-backed production value materialization eligibility
- source-backed production value row creation
- executable production graph binding
- runtime target substitution
- expected value assertion

At this entry checkpoint, only the first item is already present from v17.x.

The remaining items stay blocked.

## Required separation

Source-backed production value materialization boundary metadata is not a production value row.

A materialization boundary checkpoint may define the criteria for a future source-backed production value row, but it must not contain:

- real Beast King / Juo probability values
- real Beast King / Juo reward values
- source-backed production value rows
- source-backed numeric values
- numerator values
- denominator values
- decimal values
- payout amounts
- production fixture values
- executable probability fields
- executable reward fields
- production graph bindings
- runtime target substitutions
- expected value assertions
- expected reward assertions

## Current guarantee

A Juo source candidate can pass through the following metadata layers without becoming executable:

1. source policy
2. candidate inventory
3. review gate
4. citation metadata
5. unit metadata
6. extracted value metadata
7. production value promotion gate
8. production value draft metadata
9. production value placeholder
10. production value readiness review
11. source-backed production value preflight
12. source-backed production value draft
13. source-backed production value promotion

The v18.0 materialization boundary entry does not add a fourteenth executable layer.

It only names the next boundary after promotion and keeps materialization blocked until explicit typed fixture and guard coverage exist.

## Non-goals

This checkpoint does not add or change:

- real Beast King / Juo probability values
- real Beast King / Juo reward values
- source-backed production value rows
- production fixture values
- executable production graph
- runtime target substitution
- expected value assertions
- report identity
- report adapters
- formatters
- contribution output
- UI
- TeX
- CSS
- Android native code
- Sugoroku expected reward baseline

## Boundary

This checkpoint is documentation-only.

No implementation files are changed.

No tests are changed.

No real production values are introduced.

No source-backed production value row is introduced.

No executable graph path is introduced.

## Next step

The next step should add typed, non-executable materialization boundary fixtures linked to source-backed production value promotion rows.

That next step should still block real values, source-backed production value rows, production fixture values, executable graph bindings, runtime target substitution, and expected value assertions until guard coverage proves the separation.
